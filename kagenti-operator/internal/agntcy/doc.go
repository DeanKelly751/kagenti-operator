/*
Package agntcy provides optional proof-of-concept integration helpers for
AGNTCY (Directory, OASF validation, identity service probe) alongside the
Kagenti operator. These code paths are disabled unless the operator is
started with --enable-agntcy-poc and individual endpoints are configured.
*/
package agntcy
